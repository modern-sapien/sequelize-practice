<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>COGS Project</title>
</head>
<body>
   <div class="container">
    <div class="jumbotron dark">
      <h1>COGS</h1>
      <h3>Inventory & Cost of Goods</h3>
      <hr>
      <a href="/" class="btn btn-warning btn-lg">
        <span class="fa fa-eye"></span> Search Inventory</a>
      <a href="/add" class="btn btn-warning btn-lg">
        <span class="fa fa-plus"></span> Update Inventory</a>
      <a href="/all" class="btn btn-warning btn-lg">
        <span class="fa fa-th-list"></span> Inventory Sheets</a>
      <a href="/long" class="btn btn-warning btn-lg">
        <span class="fa fa-th-list"></span> Cost of Goods</a>
    </div>
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-header">
            Book Database
          </div>
          <div class="card-body" id="well-section">
            <div>
                {{#each inventory}}
                    {{inventory/inventory-block}}
                {{/each}}
            </div>
          </div>
        </div>
      </div>
    </div>

  </div>

  <!-- Custom Script -->
  <script src="js/clicks.js" type="text/javascript"></script>}}
</body>
</html>